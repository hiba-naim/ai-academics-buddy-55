ALTER TABLE public.users
  ADD COLUMN IF NOT EXISTS university TEXT,
  ADD COLUMN IF NOT EXISTS graduation_year INTEGER,
  ADD COLUMN IF NOT EXISTS target_gpa NUMERIC(3,2),
  ADD COLUMN IF NOT EXISTS weekly_study_goal_hours INTEGER,
  ADD COLUMN IF NOT EXISTS preferred_study_days TEXT[] NOT NULL DEFAULT '{}'::TEXT[],
  ADD COLUMN IF NOT EXISTS preferred_study_session_length INTEGER,
  ADD COLUMN IF NOT EXISTS default_semester TEXT,
  ADD COLUMN IF NOT EXISTS max_credits_per_semester INTEGER,
  ADD COLUMN IF NOT EXISTS preferred_workload TEXT,
  ADD COLUMN IF NOT EXISTS max_difficult_courses INTEGER,
  ADD COLUMN IF NOT EXISTS show_ai_recommendations BOOLEAN NOT NULL DEFAULT TRUE,
  ADD COLUMN IF NOT EXISTS enable_planner_warnings BOOLEAN NOT NULL DEFAULT TRUE,
  ADD COLUMN IF NOT EXISTS notify_assignments BOOLEAN NOT NULL DEFAULT TRUE,
  ADD COLUMN IF NOT EXISTS notify_exams BOOLEAN NOT NULL DEFAULT TRUE,
  ADD COLUMN IF NOT EXISTS notify_weekly_summary BOOLEAN NOT NULL DEFAULT TRUE,
  ADD COLUMN IF NOT EXISTS notify_registration BOOLEAN NOT NULL DEFAULT TRUE,
  ADD COLUMN IF NOT EXISTS theme TEXT NOT NULL DEFAULT 'system',
  ADD COLUMN IF NOT EXISTS compact_mode BOOLEAN NOT NULL DEFAULT FALSE,
  ADD COLUMN IF NOT EXISTS animations_enabled BOOLEAN NOT NULL DEFAULT TRUE;